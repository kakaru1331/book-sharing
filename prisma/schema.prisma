generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./book-sharing.db"
}

model book {
  book_id    Int     @id @default(autoincrement())
  title      String
  authors    String
  publishers String
  image_path String?
}
